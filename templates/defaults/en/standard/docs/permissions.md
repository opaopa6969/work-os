# Permissions & Auto-Approval
This document defines the scope of operations allowed for the AI agent.

## Allowed (Auto-Approved)
- [x] Create and edit files within the project directory
- [x] Run local tests and build commands
- [x] Update documentation in `docs/`

## Restricted (Need Approval)
- [ ] File operations outside the project directory
- [ ] Deploying to external services or issuing API keys
- [ ] Adding or removing core dependencies (significant changes)
